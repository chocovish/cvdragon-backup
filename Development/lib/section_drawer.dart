import 'package:flutter/material.dart';


class SectionDrawer extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return  Drawer(
      child: Text("Section Panel"),
    );
  }
}